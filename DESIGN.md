# 새김 배치도 — 설계 문서

축제 배치도를 만들고(주최측), 참가자가 휴대폰으로 보며 **자신의 현재 위치까지** 확인하는 웹앱.

- 지도: **카카오맵**(국내 지명·건물·도로가 한글로 정확)
- 프레임워크: Next.js 16 + React 19 + Tailwind v4
- 백엔드: Firebase (Firestore 저장, 필요 시 인증)

---

## 1. 참고 자료 분석

### 목업 이미지 (보여주는 화면의 이상형)
- **카테고리별 색 핀**: 무대=보라, 음식=주황, 화장실=파랑, 주차=초록 — 색과 아이콘으로 종류를 한눈에 구분
- **왼쪽 패널**: 검색창 + 카테고리 **필터 토글 리스트**(카테고리별 표시 on/off)
- **구역/경계**: 점선 경계, 영역 표시

### Mapus (github.com/alyssaxuu/mapus) — 기능 참고
- Leaflet + Geoman(드로잉) + Turf.js(면적/거리) + **Firebase Realtime DB(실시간)** + 구글 로그인 + OSM 검색
- 기능: 마커 · 선/경로 · 폴리곤 · 자유 드로잉 · 장소검색 · 표시 토글 · 주석 목록 · **실시간 공동편집/커서** · **GeoJSON 내보내기**

### 취사선택
| 요소 | 채택 | 이유 |
|---|---|---|
| 카테고리 색 핀 (목업) | ✅ 핵심 | 축제 배치도의 정체성 |
| 필터 토글 패널 (목업) | ✅ 핵심 | 관람 모드의 핵심 UX |
| 검색 (목업/Mapus) | ✅ | 마커 검색 + Kakao 장소검색 |
| 폴리곤/도형/구역 (Mapus) | ✅ 구현됨 | 사각형·원 완료, 선(경계)만 추가 |
| GeoJSON 내보내기 (Mapus) | 🔸 나중 | 백업/이식용, 난이도 낮음 |
| 실시간 공동편집·커서 (Mapus) | 🤔 축소 | 축제엔 과함 → 아래 설명 |
| 실시간 아바타/커서 (Mapus) | ❌ | 이 용도엔 불필요 |

---

## 2. 핵심 설계 결정 — 편집 모드 vs 관람 모드 분리

Mapus는 "모두가 함께 편집"하지만, **축제 배치도는 만드는 사람(소수)과 보는 사람(다수)이 분리**된다. 하나의 앱을 두 모드로 나눈다.

| | 편집 모드 (주최측) | 관람 모드 (참가자) |
|---|---|---|
| 접근 | 로그인 필요 (허가된 소수) | **공개 링크, 로그인 없음** |
| 역할 | 마커·구역·경계 배치/수정, 저장 | 보기 전용 |
| 핵심 기능 | 지금까지 만든 편집기 | **카테고리 필터 · 검색 · 📍내 위치(GPS)** |
| 주 기기 | PC | **휴대폰 (모바일 최적화)** |
| 라우트 | `/` 또는 `/edit/{mapId}` | `/view/{mapId}` |

> 목업의 왼쪽 패널(검색+필터)은 사실상 **관람 모드의 메인 UI**다.

---

## 3. 카테고리 시스템 (목업의 핵심)

지금은 이모지를 자유 배치하지만, **카테고리**로 구조화한다.

```
Category = { id, name, color, icon }   // 예: 무대(🎤,보라), 음식(🍔,주황),
                                        //     화장실(🚻,파랑), 주차(🅿️,초록),
                                        //     안내(ℹ️), 의무실(🚑), 캠핑(⛺)
Marker   = { 위치, categoryId }         // 색·아이콘은 카테고리에서 파생
```

이점:
- **색 핀 마커** — 카테고리 색의 물방울 핀 + 흰 아이콘(SVG). 목업 룩
- **필터 패널** — 카테고리 on/off 토글로 "화장실만 보기" (목업의 토글 리스트)
- **검색** — 이름/카테고리로 마커 찾기 + Kakao 장소검색

기본 카테고리는 프리셋으로 제공하고, 주최측이 추가/색변경 가능(확장).

---

## 4. 기능 목록

### 편집 모드 (주최측)
- [x] 카카오맵 표시
- [x] 마커(아이콘/텍스트) 배치·드래그·삭제
- [x] 구역 프리셋(사각형·원) 그리기 + 모서리 핸들 크기조절
- [x] 속성 패널(이름·색)
- [x] GPS 내 위치
- [ ] **카테고리 기반 마커**(색 핀) 로 재구성
- [ ] 선/경계(Polyline) 그리기
- [ ] Firestore 저장/불러오기
- [ ] 여러 배치도 관리(목록)
- [ ] (선택) 다중 편집 실시간 반영(Firestore onSnapshot)

### 관람 모드 (참가자)
- [ ] 공개 링크로 열림(로그인 없음), 읽기 전용
- [ ] 카테고리 **필터 토글** 패널
- [ ] **검색**(마커 이름/카테고리)
- [ ] **📍내 위치** GPS 파란 점
- [ ] 마커 탭 → 정보(이름/설명)
- [ ] 모바일 최적화(하단 시트 UI 등)

---

## 5. 데이터 모델 (Firestore)

```
maps/{mapId}
  title: string
  createdBy: string            // 이메일
  updatedAt: timestamp
  center: { lat, lng }         // 초기 지도 중심
  level: number                // 초기 줌
  categories: [
    { id, name, color, icon }
  ]
  elements: [
    {
      id, type: "marker" | "zone" | "line",
      categoryId?,             // marker
      label,
      // 위치
      lat?, lng?,             // marker
      shape?: "rect"|"circle"|"polygon",
      bounds?, radius?, path? // zone/line
    }
  ]
```

- 배치도 1개 = 문서 1개.
- 관람 모드는 이 문서를 **읽기 전용**으로 로드.

### 보안 규칙(개념)
```
maps/{mapId}
  allow read: if true;                         // 누구나 관람
  allow write: if request.auth != null
             && request.auth.token.email in [허용목록];  // 편집은 허가된 계정만
```

---

## 6. 화면 / 라우트 구조

```
/                 편집기(현재) — 추후 /edit 로 이동 가능
/edit/{mapId}     편집 모드 (로그인 필요)
/view/{mapId}     관람 모드 (공개, 모바일)
/maps             내 배치도 목록 (편집자)
```

컴포넌트(현재/예정):
```
components/
  MapEditor.tsx     편집 팔레트+상태 (있음)
  MapCanvas.tsx     카카오맵 렌더 (있음)
  MapViewer.tsx     관람 모드 지도 (예정)
  FilterPanel.tsx   카테고리 필터+검색 (예정)
lib/
  map-types.ts      요소 타입 (있음)
  categories.ts     카테고리 프리셋 (예정)
  firebase.ts       Firebase 연동 (있음)
```

---

## 7. 기술 스택

- **지도**: 카카오맵 (`react-kakao-maps-sdk`) — JS 키 + 도메인 등록 + 카카오맵 서비스 활성화
- **프레임워크**: Next.js 16(App Router) + React 19 + TypeScript
- **스타일**: Tailwind v4
- **저장/인증**: Firebase Firestore (+ 편집 모드에 구글 로그인, Realtime DB 대신 Firestore 사용)
- **GPS**: 브라우저 Geolocation API (지도 API와 무관, HTTPS 필요)
- **배포**: Firebase Hosting 등 (HTTPS 자동 → 휴대폰 GPS 사용 가능)

---

## 8. 단계별 로드맵

1. **카테고리 시스템** — 색 핀 마커 + 카테고리 팔레트로 재구성 (목업 룩)
2. **필터 패널 + 검색** — 카테고리 토글, 마커 검색
3. **Firestore 저장/불러오기** — 배치도 문서 CRUD
4. **관람 모드 `/view/{id}`** — 공개·읽기전용·모바일·GPS·필터·검색
5. **배포(HTTPS)** — 휴대폰 GPS 실사용
6. (선택) 선/경계 그리기 · GeoJSON export · 다중 편집 실시간 반영

> 현재 편집기(카카오맵·마커·도형·GPS·핸들)가 **1번의 토대**라 바로 이어진다.

---

## 9. 미래 확장 (선택)

- 다중 편집 실시간 반영(Firestore `onSnapshot`) — Mapus의 협업을 축소 적용
- 선/경로 그리기(경계·도로 점선)
- GeoJSON 가져오기/내보내기 (백업·이식)
- 카테고리 커스터마이즈(주최측이 색/아이콘 추가)
- 배경 이미지 오버레이(손그림 배치도를 지도에 덮기)
- 부스/공연 시간표 연동, 즐겨찾기, 길안내
