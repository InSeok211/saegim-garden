# legacy-map-viewer (미사용, 배포 제외)

`DESIGN.md`에 적힌 초기 설계("지도 뷰어를 Next.js `/view` 라우트로 만든다")의 산출물입니다.
축제 참가자 앱(`hosting-festival/`)이 자체 지도 UI를 갖추면서 이 뷰어는 더 이상 어디서도 참조되지 않아
`firebase.json` 배포 대상에서 제외했습니다.

나중에 다시 쓰려면:

```bash
cd legacy-map-viewer
npm install
npm run dev
```
